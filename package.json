{
  "name": "web-annotation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI-first web annotation toolkit (monorepo).",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "example": "pnpm --filter @web-annotation/example-playground run dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
